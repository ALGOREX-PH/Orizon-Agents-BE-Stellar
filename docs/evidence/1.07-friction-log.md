# 1.07 friction log — external-wallet registration run (BLO-17)

**Filled by the observer, DURING the run — not reconstructed afterward.** This
log is a **required output** of story 1.07 (AC3). Every hesitation, question,
confusion, or error the contributor hits while registering **unaided** goes in
the table below with a severity. Anything the contributor could not do without
being coached is a finding here, not a quiet save.

Findings that point at a rough onboarding edge feed **story 5.03** (onboarding /
first-run polish). Any defect (wrong behaviour, broken step, misleading UI) also
gets a **Bug** issue in Linear, linked to the story whose AC it violates.

**Run metadata (fill first):**

- Contributor (external, non-Blocksmiths): `____`
- Observer: `____`
- Date / time (ISO 8601): `____`
- Wallet software + version: `____`
- Public testnet URL used: `____`
- Agent id being registered: `____`

---

## Friction table

Severity key: **blocker** (could not proceed unaided) · **major** (proceeded but
badly slowed / nearly stuck) · **minor** (small stumble, self-recovered) ·
**none** (clean).

| Step | What happened | Friction (hesitation / question / confusion / error) | Severity | → feeds 5.03? | Bug filed? |
|------|---------------|------------------------------------------------------|----------|---------------|------------|
| Wallet install | | | | | |
| Friendbot funding | | | | | |
| Network selection (testnet vs mainnet) | | | | | |
| Open `/app/register` | | | | | |
| Agent id charset rule `[A-Za-z0-9_]{1,32}` | | | | | |
| Availability check on blur | | | | | |
| Price units (USDC vs stroops) | | | | | |
| Sign prompt in wallet | | | | | |
| Success card / copy evidence | | | | | |
| _(add rows as needed)_ | | | | | |

---

## Explicit none-statement (required by AC3)

AC3 requires **either** a list of friction points (the table above) **or** an
explicit statement that there were none. Do not leave this blank — if the table
has entries, that is the list; if it does not, state it here:

> If there were NO points of confusion, state that here: `____`

---

## Summary for the reviewer

- Total findings: `____`  (blocker: `__` · major: `__` · minor: `__`)
- Was the registration completed **unaided**? (Y / N — if N, explain): `____`
- Bugs filed (issue links): `____`
- Items forwarded to story 5.03: `____`
